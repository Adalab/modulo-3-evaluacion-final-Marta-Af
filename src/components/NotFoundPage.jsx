import { Link } from "react-router-dom";

import PropTypes from "prop-types";

function NotFoundPage() {
  return (
    <>
      <div>
        <button>
          <Link to="<List persons={filtered}" />
        </button>
      </div>
    </>
  );
}

NotFoundPage.propTypes = {};

export default NotFoundPage;
